public record Platform // record è una forma di classe equivale a scrivere una classe con le property e costruttore
(
    string PlatformId,
    string PlatformName,
    string PlatformDescription
);

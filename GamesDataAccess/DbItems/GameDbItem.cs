public record GameDbItem // record è una forma di classe equivale a scrivere una classe con le property e costruttore
(
    string GameId,
    string GameName,
    string GameDescription,
    string GameTags
);


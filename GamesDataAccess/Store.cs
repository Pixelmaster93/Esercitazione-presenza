public record Store // record è una forma di classe equivale a scrivere una classe con le property e costruttore
(
    string StoreId,
    string StoreName,
    string StoreDescription,
    string StoreUrl
);
